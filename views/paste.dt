extends layout

block body
    pre.codeblock-text
        code.hljs.d.
            string #[a id] => "#[b= id]";
            DateTime #[a createdAt] => new DateTime ("#[b= createdAt] UTC");
    
    pre
        code
            |= code