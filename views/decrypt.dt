extends baseLayout

block body
    #decrypt-paste
        .block
            h3 decrypt paste

            form(action="/" ~ id ~ "/decrypt", method="post")
                input(type="password", name="password", placeholder="password", autofocus)

                p #[button(type="submit") decrypt]&nbsp;&nbsp;&nbsp;&nbsp;#[a(href="/") cancel]
