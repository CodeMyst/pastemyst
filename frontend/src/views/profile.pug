#profile-header
    img.avatar
    p.username
    a.logout logout

ul#profile-pastes
    li.paste
        a(route="")
            p.title
            p.info
                span.created-at
                span.expires-in
