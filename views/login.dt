extends baseLayout

block body
    #login
        h2 login

        p information stored per user in the database: id, username, service (github, gitlab) id, starred pastes, url to the avatar, whether your profile is public or not, the default language and the api key.

        a.login-button(href="/login/github")
            i.fab.fa-github
            span github

        a.login-button(href="/login/gitlab")
            i.fab.fa-gitlab
            span gitlab
