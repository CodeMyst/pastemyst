module model.edit_type;

// TODO: Add all the types.

/** 
 * Type of paste edits.
 */
public enum EditType
{
    title,
    pastyTitle,
    pastyLanguage,
    pastyContent,
    pastyAdded,
    pastyRemoved
}
