#profile-header
    img.avatar
    p.username
    a.logout logout

ul#profile-pastes
    li.paste
        a(route="")
            .title
                img.lock(src="/assets/icons/lock.svg")
                p
            p.info
                span.created-at
                span.expires-in
