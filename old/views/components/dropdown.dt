details.dropdown
    summary --
    .select(aria-label=dropdownLabel)
        input(type="text", autocomplete="off", placeholder="search...", name="search", aria-label=dropdownLabel ~ " search")
        p.not-found.hidden no items found
        block options
        // example option:
        // label.option
        //     input(type="radio", name="example", value="slower", checked)
        //     span slower
