extends layout

block body
    pre
        code(class="hljs cpp") public class PasteInfo&#13;{&#13;    long #[a id] = #[b= id];&#13;    DateTime #[a createdAt] = new DateTime ("#[b= createdAt] UTC");&#13;}
    a(class="button button-primary", href="/") Create a new PasteMyst
    pre
        code
            |= code